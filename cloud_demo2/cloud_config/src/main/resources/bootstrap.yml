server:
  port: 7001
spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/wuxinyuwxy?tab=repositories
          search-paths: config_repository